hydra:
  run:
    dir: /media/dmitry/data/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

defaults:
  - model: detector_defaults
  - lr_scheduler: one-cycle
  - common_transforms

general:
  gpu: 1

data:
  train:
    type: hdf5
    dir: /media/dmitry/other/dfdc-crops/hdf5
    chunks: 5 - 50
    dir_list: ''
    sample:
      frames: 10
      real_fake_ratio: 3.33
      sparse_frames_prob: 0.5
    loader:
      batch_size: 16
      workers: 1
  val:
    type: hdf5
    dir: /media/dmitry/other/dfdc-crops/hdf5
    chunks: 0, 1, 2, 3, 4
    sample:
      frames: 10
      real_fake_ratio: 3.33
      sparse_frames_prob: 1.0
    loader:
      batch_size: 16
      workers: 1

optimizer:
  lr: 0.001
  betas: [0.9, 0.999]
  weight_decay: 0

train:
  epochs: 5
  epoch_length: 100
  checkpoints:
    load: /media/dmitry/data/outputs/2020-03-17/20-11-27/checkpoint_100.pth
    base_dir:
    interval_epoch: 1
    interval_iteration: 1000

validate:
  interval: 1

logging:
  log_iter_interval: 100
